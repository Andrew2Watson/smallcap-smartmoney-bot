# SmallCap SmartMoney Bot (Render-ready)

- Telegram bot (long-polling) + Flask keepalive for Render Web Service.
- Do NOT commit `.env`. Set environment variables in Render dashboard.

## Run locally
pip install -r requirements.txt
python app.py
